node_modules/

# Firebase Admin SDK service account key — this one IS a secret, unlike
# js/firebase-config.js. Never commit it. See scripts/seed-firebase.mjs.
serviceAccountKey.json

.DS_Store

# Per-machine local settings, not shared project config (that's launch.json).
.claude/settings.local.json

# Local-only admin tool for approving paid social listings (promote.html) —
# reviews pending bids and decides which ones had a verified payment.
# Deliberately never pushed: run it locally with `npm run admin-tool`.
admin-tool/
